# MakeDirectory Macro

Creates directories at the specified paths. Parent directories are created as needed.

## Usage
- **Macro:** `MakeDirectory`
- **Props:** `{ paths: string[] }`
- **Returns:** Success or error message for each path

## Example
```json
{
  "paths": ["~/Documents/new-folder", "~/Documents/another-folder"]
}
```
